%h1 Teams
%table.table.list-table.table-striped
  - @teams.each do |team|
    %tr
      %td= link_to "#{team.name}", team
      - if is_admin?
        %td= link_to '', edit_team_path(team), {:class => "icon icon-edit"}
        %td= link_to '', team, method: :delete, data: { confirm: 'Are you sure?' }, :class => "icon-remove"
%br/
= link_to 'Create A Team', new_team_path, {:class => "btn btn-primary btn-large"}