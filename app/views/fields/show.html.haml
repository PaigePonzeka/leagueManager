%p#notice= notice
%h1=@field.name

%div=link_to @field.park.name, @field.park

- if is_admin?
  = link_to 'Edit', edit_field_path(@field), :class => "btn btn-large"
= link_to 'View All Fields', fields_path
